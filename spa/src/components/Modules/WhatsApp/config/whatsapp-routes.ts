import { useActionRoutes } from "quasar-ui-danx";

export const whatsAppConnectionRoutes = useActionRoutes("whatsapp-connections");
export const whatsAppMessageRoutes = useActionRoutes("whatsapp-messages");